# Research Log: DB2Model Experimentation

**Дата начала:** 14 июля 2026 г.
**Цель:** Сравнение Baseline, Light-RAG и SFT по метрикам EX, VES и Token Budget на репрезентативной выборке БД из BIRD.

---

## 1. Выбор целевых баз данных (Week 1)

Выбирала бд из bird_small.json, так как для применения бенчмарка это маленьких датасет, который следует запускать всегда в начале экспериментов, чтобы не тратить много токенов. В датасете 7 баз данных: `toxicology`, `california_schools`, `card_games`, `debit_card_specializing`, `financial`, `codebase_community`, `student_club`.

### Сводная матрица для выбора целевых баз данных (оценка от 1 до 5 ⭐)

| База данных | Размер (таблиц) | Сложность JOINs | Потенциал для Light-RAG | Стабильность Baseline EX | Комментарий |
| :--- | :---: | :---: | :---: | :---: | :--- |
| **`toxicology`** | 4 | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | подходит для проверки гипотезы графа знаний |
| **`financial`** | 8 | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | классический multi-hop, репрезентативная бизнес-схема |
| **`california_schools`** | 3 | ⭐⭐ | ⭐⭐ | ⭐⭐⭐⭐⭐ | чистая схема, предсказуемые метрики для отладки пайплайна |
| `student_club` | 8 | ⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐ | Отличная замена `california_schools`, если потребуется чуть более сложная логика |
| `debit_card_specializing` | 5 | ⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐ | Хороша для проверки работы со статистиками (`pg_stats`) и строковыми датами |
| `codebase_community` | 8 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐ | может быть слишком сложной, высокий риск низкого baseline EX. Может явно показать, что RAG не подходит (и объяснить почему) |
| `card_games` | 6 | ⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐ | Специфична, акцент на текстовый поиск, мало числовых агрегаций |

### Отобранные БД (3 шт.):
1. **`toxicology`**(4 таблицы)
    - *Причина выбора:* для проверки гипотезы о графовых связях. Нестандартная предметная область (молекулы-атомы-связи) требует от модели нетривиального schema linking, что подходит для проверки правильности определения архитектуры Light-RAG.
2. **`financial`** (8 таблиц)
   - *Причина выбора:* бизнес-схема среднего размера, содержит множественные JOIN пути, что позволяет объективно оценить, насколько сокращение контекста через RAG влияет на точность сложных запросов.

3. **`codebase_community`** (8 таблиц)
   - *Причина выбора:* высокая степень связности (многие таблицы ссылаются на users и posts). Наличие сложных JOIN. 

---

## 2. Лог экспериментов

### Эксперимент 1: Baseline (Week 1)
#### Запуск 1:
- **Дата:** [17.07.2026]
- **Модель:** Qwen2.5-Coder-7B-Instruct
- **Промпт:** Zero-shot, полная схема БД в контексте.
- **Результаты:** ошибки
1. ValueError: Сгенерированный SQL не прошел валидацию. Модель Qwen2.5-Coder-7B (в zero-shot режиме, без дообучения) сгенерировала SQL-запрос, который библиотека sqlglot не смогла распарсить как корректный PostgreSQL (например, не хватает SELECT, есть опечатка в ключе или странная структура). Это нормально для baseline, есть параметр MAX_RETRIES=7, чтобы модель могла попробовать еще раз.
2. UnboundLocalError: cannot access local variable `final_result`. В файле text2sql_implementation.py метод query возвращает `final_result`, однако переменная создается только если за MAX_RETRIES сгенерируется валидный SQL запрос, в противном случае, код падает с ошибкой. Таким образом, за MAX_RETRIES=7 target модель не сгенерировала валидный SQL. **Необходимо исправить баг для получения информации о кол-ве потраченных токенов**.
   - `california_schools`: EX = 0.0, VES = 0.0, Токенов/запрос = ~XXXX
   - `toxicology`: EX = 0.0, VES = 0.0, Токенов/запрос = ~XXXX
   - `codebase_community`: EX = 0.0, VES = 0.0, Токенов/запрос = ~XXXX

### Эксперимент 1: Baseline (Week 1)
- **Дата:** [17.07.2026]
- **Модель:** Qwen2.5-Coder-7B-Instruct
- **Промпт:** Zero-shot, полная схема БД в контексте.
- **Результаты (на validation split из 20 вопросов):**
  - `california_schools`: EX = 0.XX, VES = 0.XX, Токенов/запрос = ~XXXX
  - `toxicology`: EX = 0.XX, VES = 0.XX, Токенов/запрос = ~XXXX
  - `codebase_community`: EX = 0.XX, VES = 0.XX, Токенов/запрос = ~XXXX
- **Наблюдения:** [Например: Модель часто ошибается в именах колонок при полном контексте, подтверждая необходимость дистилляции].
- **Артефакты:** `results/baseline_california_schools.csv`

### Эксперимент 2: Light-RAG (Week 3)
- **Дата:** ...
- **Изменения:** Подграф вместо полной схемы. Эмбеддинги: BAAI/bge-m3.
- **Результаты:** ...
- **Наблюдения:** ...

---

## 3. Распределение задач в команде